{
  "name": "dabanc-launchpad-protocol",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "test": "hardhat test",
    "clean": "find . -name '._*' -type f -delete && npx hardhat clean",
    "compile": "find . -name '._*' -type f -delete && npx hardhat compile",
    "deploy:local": "find . -name '._*' -type f -delete && npx hardhat run scripts/deploy_sepolia.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/better-sqlite3": "^7.6.13",
    "hardhat": "^2.22.17"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "better-sqlite3": "^12.5.0",
    "dotenv": "^16.4.5"
  }
}
