# 🚀 一键启动指南

## 超简单！只需要 3 条命令

### 1️⃣ 一键部署（首次运行）

```bash
./quick-start.sh
```

**这个命令会自动完成：**
- ✅ 清理临时文件
- ✅ 编译智能合约
- ✅ 启动 Anvil 本地节点
- ✅ 部署所有合约
- ✅ 更新配置文件
- ✅ 初始化数据库
- ✅ 设置白名单

**预计耗时：30-60 秒**

---

### 2️⃣ 启动前端

```bash
./start-frontend.sh
```

前端会运行在: **http://localhost:5173**

---

### 3️⃣ 配置 MetaMask

1. **添加 Anvil 网络**
   - 网络名称: `Anvil Local`
   - RPC URL: `http://127.0.0.1:8545`
   - Chain ID: `31337`
   - 货币符号: `ETH`

2. **导入测试账户**
   - 私钥: `0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80`
   - 地址: `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266`
   - 余额: `10000 ETH`

3. **在前端点击"连接钱包"，选择 Anvil Local 网络**

---

## 🎮 可选：启动其他服务

### 启动清算机器人（自动清算）
```bash
npx hardhat run scripts/auto_bot.ts --network localhost
```

### 启动流量模拟器（模拟出价）
```bash
npx hardhat run scripts/simulate_traffic.ts --network localhost
```

### 运行动态供应量测试
```bash
npx hardhat run scripts/test_dynamic_supply.ts --network hardhat
```

---

## 🛑 停止服务

### 停止 Anvil
```bash
./stop-anvil.sh
```

### 停止前端
按 `Ctrl+C`

---

## 🔄 重新部署

如果需要重新部署（重置所有数据）：

```bash
# 1. 停止 Anvil
./stop-anvil.sh

# 2. 重新启动
./quick-start.sh

# 3. 启动前端
./start-frontend.sh
```

---

## ⚡ 完整流程（3 个终端）

### 终端 1: 部署
```bash
./quick-start.sh
```

### 终端 2: 前端
```bash
./start-frontend.sh
```

### 终端 3: 清算机器人（可选）
```bash
npx hardhat run scripts/auto_bot.ts --network localhost
```

---

## 🎯 现在可以做什么？

1. ✅ **访问前端**: http://localhost:5173
2. ✅ **连接 MetaMask** 到 Anvil Local 网络
3. ✅ **查看供应统计**: 总量 1000万 wSPX，当前轮次 500
4. ✅ **提交出价**: 输入金额和限价
5. ✅ **观察动态调整**: 价格高 → 供应增加，价格低 → 供应减少
6. ✅ **领取代币**: 清算后 claim 代币或退款

---

## 🐛 遇到问题？

### 编译错误
```bash
npm run compile
```

### Anvil 无法启动
```bash
# 检查端口占用
lsof -i :8545

# 强制停止
./stop-anvil.sh

# 重新启动
./quick-start.sh
```

### 前端连接失败
1. 确认 Anvil 在运行: `lsof -i :8545`
2. 刷新页面
3. 检查 MetaMask 网络选择
4. 查看浏览器控制台错误

---

## 📚 详细文档

- 完整说明: `README.md`
- 动态供应量: `docs/DYNAMIC_SUPPLY_GUIDE.md`
- 安全更新: `docs/SECURITY_UPGRADE_TECHNICAL_SPEC.md`

---

**就这么简单！🎉**

