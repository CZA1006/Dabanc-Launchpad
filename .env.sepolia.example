# ============================================
# Sepolia 测试网配置
# ============================================

# === 必填：钱包和网络 ===
# 你的钱包私钥（不要暴露！从 MetaMask 导出）
PRIVATE_KEY=你的私钥

# Sepolia RPC URL（从 Alchemy/Infura 获取）
SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/你的API密钥

# === 合约地址（部署后自动填写）===
AUCTION_ADDRESS=
USDC_ADDRESS=
TOKEN_ADDRESS=
VAULT_ADDRESS=

# === Bot 配置（Sepolia 推荐值）===
# 轮询间隔（毫秒）- Sepolia 建议 5-10 秒
BOT_POLLING_INTERVAL=5000

# 清算后等待时间（毫秒）- Sepolia 建议 15 秒
BOT_POST_CLEARING_DELAY=15000

# 轮次持续时间（秒）- 默认 5 分钟
ROUND_DURATION=300

# 每轮代币供应量
TOKEN_SUPPLY_PER_ROUND=500

# 价格范围
MIN_CLEARING_PRICE=0.01
MAX_CLEARING_PRICE=1000

# === 数据库配置 ===
DB_PATH=./backend_db/orders.db
HISTORY_PATH=./backend_db/history.json
