# Dabanc Launchpad 前端

> 🚀 仪表盘风格的去中心化批量拍卖前端

## 设计理念

**核心目标**: 让用户对资金流向有掌控感

页面设计像一个**精密的飞行仪表盘**：所有关键数据（倒计时、预计价格、个人持仓、申领状态）都在不滚动页面的情况下触达用户。

---

## 🎨 视觉设计

### 配色方案

采用专业的**深色主题 (Dark Mode)**：

| 颜色变量 | 色值 | 用途 |
|---------|------|------|
| `--bg-primary` | `#0a0f16` | 主背景 |
| `--bg-card` | `#151c28` | 卡片背景 |
| `--accent-cyan` | `#00d4ff` | 主强调色/CTA |
| `--accent-green` | `#00ff88` | 成功/成交状态 |
| `--accent-red` | `#ff4757` | 失败/出局状态 |
| `--accent-yellow` | `#ffc107` | 警告/等待状态 |

### 字体系统

| 字体 | 用途 |
|------|------|
| **JetBrains Mono** | 价格、数字、代码 |
| **Space Grotesk** | 标题 |
| **Inter** | 正文 |

### 背景效果

- 网格线背景 (`background-image: linear-gradient`)
- 顶部渐变光晕 (`radial-gradient`)
- 卡片发光效果 (`box-shadow: glow`)

---

## 📐 布局架构

### 仪表盘式布局

```
┌────────────────────────────────────────────────────────────┐
│  HEADER: Logo + 网络状态 + 钱包连接                          │
├────────────────────────────────────────────────────────────┤
│  PHASE INDICATOR: 预热期 → 竞拍期 → 清算期 → 结算期           │
├────────────────────────────────────────┬───────────────────┤
│                                        │                   │
│  MAIN CONTENT:                         │  SIDEBAR:         │
│  ┌──────────────────────────────────┐  │  ┌─────────────┐  │
│  │ 指标卡片行 (4列)                  │  │  │ 实时订单簿   │  │
│  │ 状态 | 倒计时 | 清算价 | 募资额   │  │  │             │  │
│  └──────────────────────────────────┘  │  │             │  │
│  ┌──────────────────────────────────┐  │  └─────────────┘  │
│  │ 交易面板                          │  │  ┌─────────────┐  │
│  │ - 出价金额输入                    │  │  │ 我的资产    │  │
│  │ - 限价输入                        │  │  │             │  │
│  │ - 模拟计算器                      │  │  └─────────────┘  │
│  │ - 按钮组 (Approve/Submit)         │  │  ┌─────────────┐  │
│  └──────────────────────────────────┘  │  │ 项目信息    │  │
│  ┌──────────────────────────────────┐  │  │             │  │
│  │ 深度图                            │  │  └─────────────┘  │
│  └──────────────────────────────────┘  │                   │
│                                        │                   │
└────────────────────────────────────────┴───────────────────┘
```

### 响应式断点

| 断点 | 布局调整 |
|------|----------|
| > 1024px | 双栏布局 (主内容 + 侧边栏) |
| 768px - 1024px | 单栏布局，指标卡片 2列 |
| < 768px | 移动端优化，字体缩小 |

---

## ⚡ 核心功能

### 1. 四阶段状态机

| 阶段 | 状态 | 用户操作 |
|------|------|----------|
| **预热期** | 项目介绍 | 查看信息 |
| **竞拍期** | 接受出价 | 提交订单 |
| **清算期** | 计算中 | 等待 |
| **结算期** | 已结算 | 领取代币/退款 |

### 2. 价格发现

- **实时清算价**: 基于当前订单簿计算
- **深度图**: 可视化出价分布
- **模拟计算器**: 输入金额和价格，预测成交结果

### 3. 乐观更新 (Optimistic UI)

```typescript
// 用户提交后立即显示 "提交中" 状态
const optimisticBid = {
  status: 'pending',
  // ...
};
setPendingBid(optimisticBid);

// 链上确认后替换为真实数据
useWatchContractEvent({
  onLogs: () => setPendingBid(null)
});
```

### 4. 错误处理

友好的中文错误提示：

```typescript
const ERROR_MESSAGES = {
  "User denied": "您取消了交易请求",
  "insufficient funds": "账户 ETH 余额不足以支付 Gas 费用",
  "KYCRequired": "您尚未通过 KYC 认证，无法参与竞拍",
  // ...
};
```

### 5. 网络容错

- **多 RPC 节点**: 自动切换备用节点
- **降级轮询**: WebSocket 失效时切换到 Polling
- **更新时间显示**: "数据更新于 X 秒前"

---

## 🛠️ 技术栈

| 库 | 用途 |
|----|------|
| React 18 | UI 框架 |
| Vite | 构建工具 |
| TypeScript | 类型安全 |
| Wagmi v3 | 以太坊交互 |
| Viem | 底层工具库 |
| RainbowKit | 钱包连接 |
| TanStack Query | 数据缓存 |

---

## 📁 文件结构

```
dabanc-frontend/
├── index.html          # 入口 HTML (含 Meta 标签)
├── src/
│   ├── main.tsx        # React 入口
│   ├── App.tsx         # 主组件 (仪表盘布局)
│   ├── index.css       # 全局样式 (CSS 变量)
│   ├── constants.ts    # 合约 ABI & 配置
│   ├── wagmi.ts        # Wagmi 配置
│   └── vite-env.d.ts   # 类型声明
├── package.json
├── tsconfig.json
└── vite.config.ts
```

---

## 🚀 快速开始

### 安装依赖

```bash
cd dabanc-frontend
npm install
```

### 本地开发

```bash
npm run dev
```

访问 http://localhost:5173

### 生产构建

```bash
npm run build
npm run preview
```

---

## 🔧 配置说明

### 合约地址

编辑 `src/constants.ts`:

```typescript
export const AUCTION_ADDRESS = "0x...";
export const USDC_ADDRESS = "0x...";
```

### RPC 节点

编辑 `src/wagmi.ts`:

```typescript
const SEPOLIA_RPC_URLS = [
  'https://your-alchemy-url.com',
  // 备用节点...
];
```

### WalletConnect

获取 Project ID: https://cloud.walletconnect.com

```typescript
projectId: 'your-project-id',
```

---

## 📋 检查清单

- [x] 响应式布局 (移动端适配)
- [x] 深色主题
- [x] 等宽字体显示数字
- [x] 实时价格更新
- [x] 乐观 UI 更新
- [x] 友好错误提示
- [x] 多节点容错
- [x] 四阶段状态机
- [x] 模拟计算器
- [x] 深度图
- [ ] WebSocket 实时推送 (待实现)
- [ ] 虚拟滚动 (大数据量优化)
- [ ] PWA 支持
- [ ] 多语言

---

## 📝 更新日志

### v2.0.0 (2024-12)

- 🎨 全新仪表盘风格 UI
- ⚡ 添加四阶段状态指示器
- 📊 添加深度图和模拟计算器
- 🔄 支持乐观更新
- 🛡️ 改进错误处理
- 📱 响应式布局优化
- 🌐 多 RPC 节点容错

---

*Made with ❤️ by Dabanc Team*
